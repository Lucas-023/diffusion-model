# Ambiente: CUDA 12.2 (driver). Instale o PyTorch a partir do índice cu121
# (não existe wheel oficial "cu122"; as wheels cu121 são compatíveis com
# drivers CUDA 12.2, que mantêm compatibilidade retroativa):
#
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121

# --- Core (treino, tensores, AMP) ---
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1

# --- Encoders de condicionamento ---
transformers>=4.40,<5      # CLIP ViT-L/14 (attribute_classifier) e ViT-B/32 (image cond.)
insightface==0.7.3         # ArcFace (identidade) + face_align (alinhamento facial)
onnxruntime-gpu>=1.17,<1.20   # backend de inferência do insightface (GPU)
                               # >=1.20 passou a exigir CUDA 13 (libcudart.so.13)
                               # por padrão no PyPI; a VM tem driver CUDA 12.2

# --- Métricas ---
lpips>=0.1.4                # perceptual similarity
scipy>=1.11                 # scipy.linalg.sqrtm usado em utils/fid.py (FID)

# --- Dados / utilitários ---
numpy>=1.26,<2               # compatibilidade com onnxruntime/insightface (evitar numpy 2.x)
pandas>=2.0
pillow>=10.0
matplotlib>=3.8
tqdm>=4.66

# --- Logging / visualização de treino ---
tensorboard>=2.16

# --- Notebooks (análise de resultados) ---
jupyter>=1.0
ipykernel>=6.29
